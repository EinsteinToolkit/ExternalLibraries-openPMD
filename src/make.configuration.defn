# make.configuration.defn file for thorn openPMD

# Define standard openPMD utilities and install those that we could build/that are
# present in the system
STD_OPENPMD_UTILS = 
ALL_UTILS += $(STD_OPENPMD_UTILS)
